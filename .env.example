# Documentation only: export these variables in your shell or hosting service.
# Django does not load this file automatically.
DJANGO_DEBUG=0
DJANGO_SECRET_KEY=
DJANGO_ALLOWED_HOSTS=
# Use 1 only behind a trusted HTTPS proxy that replaces X-Forwarded-Proto.
DJANGO_TRUST_PROXY=0
